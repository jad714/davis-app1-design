/*
 *  UCF COP3330 Fall 2021 Application Assignment 1 Solution
 *  Copyright 2021 Joshua Davis
 */

package app;

import java.util.ArrayList;
import java.util.List;

public class AddDeleteList {
    // Declare instance variable of the filename given for the list, if any, as well as the list name. Both Strings.
    public AddDeleteList(){
        // Initialize instance variable.
    }

    public List<String[]> createList(){
        // Creates a list with the given name passed in and initializes empty values to all.
        List<String[]> temp = new ArrayList<>();
        return temp;
    }

    public void blankInterface(){
        // Removes everything from the interface display (like clearing off your desk, as opposed to emptying its drawers).
    }
}
